services:
  web:
    build: .
    container_name: nyaymitra
    command: python app.py
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./uploads:/app/uploads
      - ./data:/app/data
    restart: unless-stopped